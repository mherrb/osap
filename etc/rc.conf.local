ntpd_flags="-s"
httpd_flags="-u -DSSL"  # for osap perl scripts
dhcpd_flags="-L leased -C osap_users -A abandonned"
named_flags=
sendmail_flags="-L sm-mta -bd -q30m"
pf=YES                  # Packet filter / NAT
